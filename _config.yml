# Site settings
title: login.gov
description: Welcome to the Login.gov
baseurl: '/'

# Pages
collections:
  pages:
    output: true
    permalink: /:path/

# Build settings
kramdown:
  hard_wrap: true

# Defaults
defaults:
  - scope:
      path: ""
    values:
      layout: main
