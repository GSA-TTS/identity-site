cache: bundler
language: ruby
before_install:
  - wget https://raw.githubusercontent.com/creationix/nvm/v0.31.0/nvm.sh -O ~/.nvm/nvm.sh
  - source ~/.nvm/nvm.sh
  - nvm install 6
  - node --version
install:
  - make clean
  - make setup
script: make test
